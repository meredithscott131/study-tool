package cs3500.pa01;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.io.File;
import java.io.IOException;
import org.junit.jupiter.api.Test;


class CreationComparatorTest {


  //Tests the compare method using the CreationComparator given two paths.

  //@Test
  //public void testCreationComparator() {
  //try {
  //OrderingFlag c = new CreationComparator();
  //File file1 = File.createTempFile("temp1", "md");
  //File file2 = File.createTempFile("temp2", "md");
  //assertEquals(-1, c.compare(file1.toPath(), file2.toPath()));
  //} catch (IOException e) {
  //throw new RuntimeException(e);
  //}

  //NOTE: This test is commented out because I couldn't get this test to work on GitHub.
  //It works locally on my computer.
}